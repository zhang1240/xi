# LocalLedger 下一阶段开发计划

当前版本：`2.0-axis`

下一版本：`2.1-flux`

## 1. 扩展自动记账来源

### 1.0 自动记账流水线约束

所有数据源必须遵循以下顺序：

```text
接收事件
  ↓
来源过滤
  ↓
交易完成信号检查
  ↓
金额/商户解析
  ↓
稳定去重指纹
  ↓
Room 事务写入
  ↓
仅新交易发送一次通知
```

禁止在去重或数据库写入之前发送通知。

无障碍来源额外限制：

- 只处理微信和支付宝；
- 只接受支付成功、交易成功、实付、到账、退款成功等明确信号；
- 同一应用 1.5 秒节流；
- 同一应用 10 秒最多处理 4 个有效事件；
- 同一应用 10 秒最多发送 3 条自动通知；
- 超过限制直接丢弃事件或抑制通知，不创建重复账单；
- 去重指纹不包含变化频繁的窗口文本和窗口 ID；
- 商户名只作为补全字段，不能导致重复交易。
- 当前无障碍模式只允许补全已有通知交易，不单独创建候选；独立无障碍记账需后续增加明确的支付结果页识别和用户确认。

### 1.1 支付应用通知

继续使用 `NotificationListenerService`，建立统一来源注册表：

```text
PaymentSource
├── 微信
├── 支付宝
├── 银行 App
├── 云闪付
├── 京东金融
└── 其他支付 App
```

每个来源需要定义：

- 包名
- 显示名称
- Parser
- 金额规则
- 商户规则
- 收入/支出关键词
- 去重策略
- 脱敏测试样本

建议优先支持：

1. 银行 App 通知
2. 云闪付
3. 京东金融
4. 常见银行 App

银行来源优先使用通知监听，不直接读取银行 App 界面。

### 1.2 短信来源

短信属于高敏感数据，默认不申请 `READ_SMS`。

实施顺序：

1. 优先读取短信 App 发布的通知；
2. 用户主动开启后，再评估短信读取权限；
3. 只有确有必要时，评估成为默认短信处理程序。

短信功能必须满足：

- 只识别银行交易短信；
- 不保存完整短信原文；
- 不读取验证码、聊天和联系人短信；
- 不上传短信内容；
- 使用来源 Hash 去重；
- 无法确定时进入待确认。

### 1.3 测试

新增测试：

```text
BankNotificationParserTest
SmsTransactionParserTest
ChinaUnionPayParserTest
MultiSourceDeduplicationTest
SourceDetectorTest
```

每个来源都使用脱敏样本，并验证：

- 金额
- 商户
- 交易类型
- 账户
- 置信度
- 去重结果

## 2. 关于页与 GitHub 开源

### 2.1 关于页

设置页增加：

```text
设置
└── 关于 LocalLedger
```

关于页包括：

- 应用名称
- 当前版本和构建号
- 应用简介
- 本地隐私原则
- 数据存储说明
- 开源许可证
- GitHub 项目地址
- 第三方依赖许可证
- 联系方式
- 通知监听状态
- 无障碍备选状态

### 2.2 MIT 开源文件

仓库根目录增加：

```text
LICENSE
README.md
CONTRIBUTING.md
SECURITY.md
THIRD_PARTY_NOTICES.md
```

发布前检查：

- 删除 `local.properties`；
- 不提交签名密钥；
- 不提交密码、Token 或私有配置；
- 不提交真实账单和通知内容；
- 测试样本全部脱敏；
- README 说明本地运行和隐私边界；
- 说明短信权限和无障碍权限均为可选；
- 添加 MIT License；
- 添加 Release Notes；
- 添加 Issue 模板。

### 2.3 分支建议

```text
main
develop
feature/source-bank
feature/source-sms
feature/about
feature/github-open-source
feature/theme-presets
```

## 3. 多风格主题系统（后续保留）

> 状态：保留规划，当前版本和 1.3 不实施。待来源扩展、关于页、开源发布和核心稳定性完成后再启动。

建立统一主题预设：

```text
ThemePreset
├── Classic
├── Dark
├── Industrial
└── IndustrialSciFi
```

每个主题包含：

- Material 3 ColorScheme
- Typography
- Shape
- Card 风格
- NavigationBar 风格
- Icon 风格
- 背景材质
- 动效参数

设置页增加：

```text
外观
├── 跟随系统
├── 浅色
├── 深色
├── 经典账本
└── 工业科幻
```

### 3.1 工业科幻风格边界

可以采用受工业科幻作品启发的原创风格：

- 深灰、钢蓝、琥珀色强调色；
- 工业仪表盘布局；
- 网格和结构线；
- 技术标签和状态条；
- 工程化数据排版；
- 轻量扫描线和面板动效。

不直接使用第三方作品的：

- Logo
- 角色
- 字体
- 贴图
- 图标
- 音乐
- UI 截图
- 官方品牌名称

应用内主题名称建议使用：

```text
工业科幻
Industrial Ledger
Field Console
```

### 3.2 主题验收标准

- 支持浅色和深色模式；
- 支持动态字体；
- 支持高对比度；
- 支持减少动画；
- 不影响账单扫描和金额识别；
- 不改变数据逻辑；
- 不依赖网络素材。

## 4. 版本路线

### 1.3

- 银行 App 通知来源；
- 云闪付通知来源；
- 短信通知来源；
- 关于页；
- MIT License；
- README 和隐私说明；
- GitHub 初次发布。
- 月度支出进度功能；

### 1.3 月度支出进度

用户可以填写每月可用总额，首页显示当月已支出占比：

```text
当月支出 ÷ 用户填写的月度总额 = 支出进度
```

功能要求：

- 总额由用户手动填写；
- 金额使用最小货币单位 `Long` 保存；
- 每月自动使用当月交易重新计算；
- 月度总额可以随时修改；
- 支出为 0 时显示空进度；
- 支出达到总额时显示 100%；
- 超过总额时允许显示超额状态，不截断真实金额；
- 总额未填写时显示“设置本月总额”；
- 不把账户资产余额误当成月度支出预算；
- 支持浅色、深色和动态取色；
- 支持动态字体和无障碍读屏；
- 动画支持减少动画设置。

### 1.3 波浪进度视觉

使用 Google Material 风格的波浪进度视觉作为表现层：

- 波浪只表示进度变化，不参与金额计算；
- 进度变化时使用平滑、可中断的动画；
- 正常状态使用主题主色；
- 接近总额时使用警示色；
- 超额时使用错误色并显示超额金额；
- 不使用外部网络图片或在线资源；
- 保留普通进度文本，确保低动画和无障碍场景仍可理解。

### 1.4（预留，不承诺）

- 主题预设系统（保留）；
- 经典主题（保留）；
- 工业科幻主题（保留）；
- 深色模式细节优化（保留）；
- 主题切换动画（保留）；
- 减少动画设置（保留）。

### 1.5

- 更多银行 Parser；
- 直接短信读取评估；
- 本地账单导入；
- 更复杂的商户规则；
- Widget；
- 更详细的账户统计。

## 5. 总体原则

- 短信和无障碍默认关闭；
- 银行来源优先使用通知监听；
- 低置信度不自动入账；
- 所有原始内容尽量短暂处理；
- 开源前清理敏感文件；
- 工业科幻主题使用原创资产；
- 不添加网络、广告、统计或在线 AI 服务。
